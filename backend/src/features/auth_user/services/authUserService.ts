
const createUser = async (userData:CreateUserDTO) => {

    try {
        
        console.log(userData);
        

    } catch (error) {
        
    }

}

export const authUserService = {
    createUser
};